@page
@model Presentation.Pages_Planes.EditModel

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>Plane</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="Plane.PlaneId" />
            <div class="form-group">
                <label asp-for="Plane.SeatNum" class="control-label"></label>
                <input asp-for="Plane.SeatNum" class="form-control" />
                <span asp-validation-for="Plane.SeatNum" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Plane.MakerId" class="control-label"></label>
                <select asp-for="Plane.MakerId" class="form-control" asp-items="ViewBag.MakerId"></select>
                <span asp-validation-for="Plane.MakerId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-page="./Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
